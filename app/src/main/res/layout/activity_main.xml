<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    tools:context="com.vaadin.polymer.demo.client.sampler.ai_trader.MainActivity">

    <TextView
        android:id="@+id/sample_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Hello World!" />

    <Button
        android:text="Stream @xAPI"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/buttonStream"
        android:layout_below="@+id/buttonLogin"
        android:layout_alignParentStart="true"
        android:onClick="streamLoader"/>

    <Button
        android:text="Login @xAPI"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/buttonLogin"
        android:layout_alignTop="@+id/editTextTime"
        android:layout_toEndOf="@+id/login_output"
        android:layout_marginTop="79dp" />

    <ToggleButton
        android:text="SelectTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="22dp"
        android:id="@+id/toggleButton"
        android:layout_below="@+id/sample_text"
        android:layout_toEndOf="@+id/login_output"
        android:checked="false"
        android:fontFamily="sans-serif-medium" />

</RelativeLayout>
